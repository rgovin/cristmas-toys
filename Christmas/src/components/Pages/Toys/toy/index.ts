import { Data, Toy as IToy } from '../../../../types/types';import { templateEngine } from '../../../Services/templateEngine';import toyTemplate from './template.html';import { Component } from '../../../Component';export class Toy extends Component {  constructor(toy: IToy) {    super(toyTemplate, toy);  }  view(){    return templateEngine(toyTemplate, this as unknown as Data)  }}